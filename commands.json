{

    "info": {
        "description": "Displays bot version and information"
    },

    "add": {
        "description": "Adds a new Minecraft server",
        "permissions": {
            "dm": false,
            "requires": [
                "ManageGuild"
            ]
        }
    },

    "list": {
        "description": "Lists your servers without pinging them",
        "permissions": {
            "dm": false
        }
    }

}